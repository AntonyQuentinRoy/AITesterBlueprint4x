- Uses a skill-file-driven workflow: maintains a `.commandcode/skills/` directory with numbered, reusable instruction files (e.g., `ch_01_anti_hallucination.md`, `ch_02_ricepot_testplangenerator.md`) and expects the AI to read and apply referenced skill files when composing a task. Confidence: 0.7
- Manages skills via the `cmdc skills` CLI, installing them from GitHub repos (e.g., `cmdc skills add JuliusBrussee/caveman`) into `.commandcode/skills/`; the CLI skips already-existing skills unless forced. Confidence: 0.65
- Values anti-hallucination rigor: expects outputs to clearly separate verified facts from inferences, explicitly label unknowns and assumptions, document limitations, and include self-validation that nothing was invented. Confidence: 0.8
- Uses GitHub for source code hosting (personal account `AntonyQuentinRoy`; jobkit repo `github.com/AntonyQuentinRoy/AITesterBlueprint4x`) and prefers HTTPS remote URLs over SSH. Confidence: 0.7
- Wants completed work delivered as a pull request: put changes on a feature branch (e.g., `fix/<topic>`), commit, push, and open a PR against `develop` — not just leave them as local commits. Confidence: 0.65
- Uses `develop` as the default/primary branch name instead of `main` or `master`. Confidence: 0.6
- Works on a Selenium + TestNG test automation project structured with the Page Object Model (POM) pattern (Java ecosystem). Confidence: 0.7
- Develops on Windows; the user profile path contains a space (`C:\Users\Shirley Rachel`), so generated shell commands and scripts must quote paths robustly. Confidence: 0.7
- Uses n8n (self-hosted workflow automation) installed as a global npm package on Windows; runs it as a routine background service on localhost:5678 and expects the agent to start it (`n8n start`), wait for the port to listen, and verify via the `/healthz` endpoint — same agent-managed local-service pattern as Langflow. Confidence: 0.7
- Runs Langflow locally as a background service on localhost:7860 (pip-installed Python package on Windows); expects the agent to start it, health-check it, and restart it autonomously when it dies. Confidence: 0.55
- Treats the "job tracker" app (`D:\AI_4XBatch_Docs_Projects\AITesterBlueprint4x\chapter5_jobtracker`, Vite dev server on localhost:5173) as another agent-managed local service: when asked to "open" it, start `npm run dev` in the background, verify the port is listening, then open the URL in the default browser. Confidence: 0.6
- Kicks off work with dense, prose-free pastes of complete raw context — error output/stack traces, terse symptom notes (e.g., "connection refused"), or full task inputs (an entire job description plus absolute file paths) — expecting autonomous end-to-end execution without clarification questions. Confidence: 0.7
- Is a senior QA/test-automation professional (resume name: Antony Quentin Roy, ~20 years experience) actively job-hunting for Test Automation Architect roles; maintains a resume-tailoring "jobkit" project (`AITesterBlueprint4x`, chapter-based skills) and keeps career artifacts such as the master resume under `d:\Job_Data`. Confidence: 0.6
- For new frontend app builds, specifies a modern React stack: React 18+ with functional components and hooks, Vite for build tooling, and Tailwind CSS for styling. Confidence: 0.8
- Prefers local-first browser apps — all data persisted client-side in IndexedDB via the `idb` wrapper library, with no backend, authentication, or external API calls. Confidence: 0.7
- Wants a clean, minimal, professional UI with a "premium" feel (references Linear/Trello-minimal aesthetics); for drag-and-drop interfaces prefers `@dnd-kit/core`. Confidence: 0.7
- Machine environment quirk: the user's shell sets NODE_ENV=production, so a plain `npm install` silently skips devDependencies — use `npm install --include=dev` when installing project tooling. Confidence: 0.8
- Wants apps delivered with realistic sample/seed data pre-loaded — covering every status/category and with all fields populated — so the UI can be evaluated immediately without manual data entry. Confidence: 0.6
- Prefers strong visual color-coding for statuses/categories using conventional semantic colors (red = rejected/negative, green = selected/positive, yellow = on-hold/warning), applied distinctly across UI elements. Confidence: 0.65
- Deploys frontend apps to Vercel (explicitly requested "deploy jobtracker in vercel"); expects the agent to handle the CLI-based deploy flow end-to-end. Confidence: 0.6
- Null-safety rule for code written/reviewed: when a function returns null as a deliberate "no answer" signal, every call site must explicitly handle the null case — pay special attention to JSX template literals, where null silently renders as the string "null". Confidence: 0.9
- Single-source-of-truth rule for derived UI values: when the same quantity is computed in two places (e.g., total count vs filtered count), render both from the same source of truth so they cannot drift. Confidence: 0.9
- Expects destructive bulk operations to be guarded at least as strongly as single-item ones: any irreversible multi-record action (e.g., import that replaces all rows) must reuse the existing confirmation dialog and state exact counts ("replace all 20 jobs with 8 from the file"); when importing/restoring data, offer a merge option alongside replace. Confidence: 0.9
- Expects browser-dependent code to be cross-browser-safe and actually verified beyond Chrome (e.g., open it once in Firefox): file downloads must append the anchor to the document, click, remove it, and defer URL.revokeObjectURL via setTimeout(0). The user is training as an SDET and frames cross-browser verification as a core job habit, "a testing habit more than a code fix." Confidence: 0.85
- Git hygiene: build output (dist/) and dependencies (node_modules/) must never be committed; every project folder needs its own .gitignore (node_modules/, dist/, .env) — copy the habit consistently across the chapter-based project folders — and after adding it, untrack already-committed files with `git rm -r --cached`. Confidence: 0.9
- Wants proactive privacy warnings when personal data (resume PDFs, phone numbers, home addresses) is committed to public GitHub repos, since public repos get search-engine indexed. Confidence: 0.6
- Gives feedback as mentor-style code reviews: each issue explains root cause, user-facing impact, and the general principle to retain, then ends with a terse directive ("fix this in <folder>"); expects all issues implemented end-to-end without further prompting. Confidence: 0.7
- Uses Atlassian Jira Cloud for issue tracking (instance `roy3110.atlassian.net`, account email `roy3110@gmail.com`, "KAN" project board); Jira integration code (e.g., chapter3 `jira_connector.py`) reads credentials from project `.env` files via `JIRA_URL` / `JIRA_EMAIL` / `JIRA_TOKEN`. Never record the token itself. Confidence: 0.6
